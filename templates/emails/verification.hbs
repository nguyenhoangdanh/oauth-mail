<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Verify Your Email | {{appName}}</title>
  <style>
    body {
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #ffffff;
    }

    .header {
      text-align: center;
      padding: 20px 0;
      border-bottom: 1px solid #eeeeee;
    }

    .header img {
      max-height: 50px;
    }

    .content {
      padding: 20px 0;
    }

    .verification-code {
      font-size: 24px;
      font-weight: bold;
      text-align: center;
      margin: 20px 0;
      letter-spacing: 2px;
      color: #4F46E5;
    }

    .button {
      display: inline-block;
      background-color: #4F46E5;
      color: white !important;
      text-decoration: none;
      padding: 12px 24px;
      border-radius: 4px;
      font-weight: bold;
      margin: 20px 0;
      text-align: center;
    }

    .footer {
      text-align: center;
      color: #888888;
      font-size: 12px;
      padding: 20px 0;
      border-top: 1px solid #eeeeee;
    }

    .note {
      font-size: 14px;
      color: #666;
      font-style: italic;
      margin-top: 30px;
    }

    @media (prefers-color-scheme: dark) {
      body {
        background-color: #1f2937;
        color: #e5e7eb;
      }

      .container {
        background-color: #111827;
      }

      .footer {
        color: #9ca3af;
        border-top-color: #374151;
      }

      .header {
        border-bottom-color: #374151;
      }

      .verification-code {
        color: #818cf8;
      }

      .note {
        color: #9ca3af;
      }

      a:not(.button) {
        color: #93c5fd;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="header">
      <h1>Verify Your Email Address</h1>
    </div>
    <div class="content">
      <h2>Hello {{name}},</h2>
      <p>Thank you for registering with {{appName}}. To complete your registration and verify your email address, please
        click the button below:</p>

      <div style="text-align: center;">
        <a href="{{verificationLink}}" class="button">Verify Email Address</a>
      </div>

      <p>If the button above doesn't work, you can also copy and paste the following URL into your browser:</p>
      <p style="word-break: break-all;"><a href="{{verificationLink}}">{{verificationLink}}</a></p>

      <p>This link will expire in 24 hours for security reasons.</p>

      <p>Once your email is verified, you'll have full access to all of {{appName}}'s features.</p>

      <p class="note">If you did not sign up for {{appName}}, please ignore this email or contact our support team at <a
          href="mailto:{{supportEmail}}">{{supportEmail}}</a>.</p>
    </div>
    <div class="footer">
      <p>© {{currentYear}} {{appName}}. All rights reserved.</p>
      <p>
        <a href="{{appUrl}}/privacy">Privacy Policy</a> |
        <a href="{{appUrl}}/terms">Terms of Service</a>
      </p>
    </div>
  </div>
</body>

</html>